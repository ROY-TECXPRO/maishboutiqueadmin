# Copy this file to .env.migration and fill in real values.
# .env.migration must NEVER be committed — it contains the service role key,
# which bypasses all Row Level Security. Add .env.migration to .gitignore.

SUPABASE_URL=https://crbtwikhkqbhqkimyqay.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-project-settings-api
